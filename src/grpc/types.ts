/**
 * gRPC Types (manual skeleton). For full generation, run protoc with ts plugin.
 */
export interface HealthReq {}
export interface HealthResp { status: string }

// Extend with service-specific request/response interfaces as needed.
